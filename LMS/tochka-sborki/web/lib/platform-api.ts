// Адрес platform-API (воркер auth/progress/admission). Курсы школы живут на своих
// доменах, где /api/* не маршрутизируется, поэтому запросы уходят кросс-доменно на
// основной хост (CORS воркера его разрешает). Значение вынесено из компонента:
// хардкод хоста мешал проверять гейт на локальном стенде.
// Пустая строка = относительные пути (тот же домен).
export const PLATFORM_API = process.env.NEXT_PUBLIC_PLATFORM_API ?? 'https://ai.synergify.com'
